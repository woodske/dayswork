namespace Dayswork.Tests.Generators;

// Foundation units add domain generators here per PBT-07 (centralized, reusable).
// U-03 adds ConfigSnapshotGen; U-04 adds ZoneGen + TileCoordGen; U-06 adds ContractGen.
// Reference via Arb.From<T>() or [Property(Arbitrary = new[] { typeof(DaysworkGenerators) })].
public static class DaysworkGenerators
{
}
